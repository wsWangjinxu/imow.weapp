<!--components/index/navCircle.wxml-->
<view class="shopTitle">
  <view class="top">
    <view>
      <checkbox></checkbox>  <text class="icon">工厂店</text><navigator url="{{url}}">{{shopName}}</navigator>
    </view>  
    <text wx:if="{{isOrder}}" style="font-size:24rpx;">正在分批发货中</text>
  </view>
  <view wx:if="{{isOrder}}" class="orderNum">订单号：73255569876</view>
</view>

