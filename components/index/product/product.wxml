<navigator url="{{ctn.url}}" class="product" redirect hover-class="className">
  <image src="{{ctn.imgSrc}}" />
  <text>{{ctn.title}}</text>
  <view>
    <text>￥{{ctn.price}}起</text>
    <text>已售{{ctn.number}}件</text>
  </view>
</navigator>

